#ifndef QUARKROOT_H
#define QUARKROOT_H
/*
#include <QObject>
#include <QQmlEngine>

#include "quarkenvironment.h"

class QuarkRoot : public QObject
{
    Q_OBJECT
    Q_PROPERTY(QuarkEnvironment environment READ environment CONSTANT)

public:
    explicit QuarkRoot(QObject *parent = nullptr);

    void addQmlEngine(QQmlEngine* engine);

signals:

public slots:
private:
    QuarkEnvironment m_environment;
    QHash<>
};
*/
#endif // QUARKROOT_H
