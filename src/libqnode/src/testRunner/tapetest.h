#ifndef TAPETEST_H
#define TAPETEST_H

#include <QString>
/*
class TapeTestCase : public QObject {
  Q_OBJECT
  explicit TapeTestCase(QString path, QObject* parent = 0);
  void run();

 signals:
  void line(int index, bool success, const QString& description,
            const QString& diagnostics);
};
*/
#endif  // TAPETEST_H
